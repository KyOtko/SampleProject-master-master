<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SampleProject.Activity.SearchPage">
    <ContentPage.Content>
        <Grid Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <SearchBar Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" 
                       Placeholder="Look" BackgroundColor="Transparent" 
                       HorizontalOptions="Fill"/>
            <StackLayout Grid.Row="1">
                <Label Text=" Store Picture" VerticalOptions="CenterAndExpand" Margin="150,0,0,0"/>
            </StackLayout>

            <StackLayout Grid.Row="2">
                <Label Text=" Store Picture" VerticalOptions="CenterAndExpand" Margin="150,0,0,0"/>
            </StackLayout>

            <StackLayout Grid.Row="3">
                <Label Text=" Store Picture" VerticalOptions="CenterAndExpand" Margin="150,0,0,0"/>
            </StackLayout>

        </Grid>
    </ContentPage.Content>
</ContentPage>